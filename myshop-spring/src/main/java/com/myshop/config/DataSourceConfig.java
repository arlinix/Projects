package com.myshop.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataSourceConfig {
    // Using spring.datasource.* in application.properties — no explicit bean required.
}
